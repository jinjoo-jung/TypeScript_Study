type TypeUser = {
  name: string;
  age: number;
  isValid: boolean;
};

interface InterfaceUser {
  name: string;
  age: number;
  isValid: boolean;
}

const heropy: InterfaceUser = {
  name: "Heropy",
  age: 20,
  isValid: true,
};
